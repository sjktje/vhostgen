[x] Make perrorf use a static buffer instead of mallocing.
[x] Create vhostgen --list %string%
[x] Create vhostgen --delete %string%
    Example: vhostgen --delete %ankeborg% <-- delete vhosts matching "ankeborg"
[ ] Make vhostgen --delete %string% first query the database for entries 
    matching %string%, then ask the user which ones to delete.
[x] Use mysql_real_escape_string() on user input going into db.
[x] Create vhostgen --version
[x] Make vhostgen use syslog
